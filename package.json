{
  "name": "psd-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Google Sheets → Photoshop template automation for hotel social content",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @psd-automation/shared && npm run build -w @psd-automation/schema-linter && npm run build -w @psd-automation/orchestrator && npm run build -w @psd-automation/tray && npm run build -w @psd-automation/ps-api-worker",
    "lint-schema": "tsx packages/schema-linter/src/cli.ts",
    "render": "npm run render -w @psd-automation/orchestrator --",
    "queue": "npm run queue -w @psd-automation/orchestrator --",
    "watch-jobs": "npm run watch -w @psd-automation/tray --",
    "test": "npm run test -w @psd-automation/shared && npm run test -w @psd-automation/orchestrator && npm run test -w @psd-automation/schema-linter",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "exceljs": "^4.4.0"
  }
}
