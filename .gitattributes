# Track large binary fixtures with Git LFS
*.psd filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Keep shell / Mac double-click launchers as LF (bash)
*.command text eol=lf
*.sh text eol=lf
setup.ps1 text eol=crlf
*.bat text eol=crlf
